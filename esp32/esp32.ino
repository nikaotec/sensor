#include "src/main.h"

// Este arquivo agora serve apenas como ponto de entrada para o Arduino IDE.
// Toda a lógica reside em src/ e o loop principal em src/main.cpp.

void setup() { firmware_setup(); }

void loop() { firmware_loop(); }
